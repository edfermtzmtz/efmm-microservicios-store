# ============================================== ##          APPLICATION# ============================================== #spring:  application:    name: config-service  cloud:    config:      server:        git:          uri: https://github.com/edfermtzmtz/microservicios-store.git          # Forzar extracción en repositorios de Git          force-pull: true          searchPaths: 'config-data'          username: ${GIT_USER}          password: ${GIT_PASSWORD}          default-label: discovery-test          deleteUntrackedBranches: true # Mantener ramas remotas y loccales             security:    user:      name: root      password: s3cr3t      # ============================================== ##          SERVER# ============================================== #      server:  port: 8081        